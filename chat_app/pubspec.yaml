name: chat_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  app_minimizer: ^1.0.0+2
  flutter:
    sdk: flutter
  get: ^4.6.6
  google_fonts: ^6.1.0
  google_nav_bar: ^5.0.6
  iconsax: ^0.0.8

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
